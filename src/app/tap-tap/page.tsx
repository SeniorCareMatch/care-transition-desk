import { TapTapDemo } from "@/components/tap-tap-demo";

export const metadata = {
  title: "Tap, Tap, Done — Elderly Care Match",
  description: "An EHR-style two-tap handoff concept for hospital discharge planners.",
};

export default function TapTapPage() {
  return <TapTapDemo />;
}
