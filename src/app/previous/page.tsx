import { PreviousDesk } from "@/components/previous-desk";

export const metadata = {
  title: "Care Transition Desk — earlier version",
};

export default function PreviousVersion() {
  return <PreviousDesk />;
}
